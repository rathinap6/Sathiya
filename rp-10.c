#include <stdio.h>
#include<math.h>
void main()
{
  int i,n,c;
  printf("enter value for i:");
  scanf("%d",&i);
   printf("enter value for power:");
   scanf("%d",&n);
   c=pow(i,n);
  printf("the number of digits in integer:%d",c);
}
  
